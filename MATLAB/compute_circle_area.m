%Andrew Baldwin and Samuel Cory
function [circle_area] = compute_circle_area(radius)
%Calculate the area of a circle given its radius
%Parameters:
    %radius (float)
%Returns:
    %circle_area (float)
%Area = Pi*R^2
circle_area = pi * radius ^2;
end

